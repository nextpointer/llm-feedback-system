{
  "name": "llm-feedback-system",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "dev:client": "bun run --filter client dev",
    "dev:server": "bun run --filter server dev",
    "dev": "bun run --filter '*' dev"
  },
  "type": "module",
  "workspaces": [
    "apps/*"
  ]
}
